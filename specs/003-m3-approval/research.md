# 研究记录：M3 审批

## 决策

| 主题 | 结论 |
|------|------|
| 分级启发式 | 首版用关键字表（如 `git push`、`rm -rf`、`pip install`）+ 默认 L2；可后续换 ML/规则引擎 |
| L3 自动通过 | **禁止**：代码路径显式拒绝 |
| 飞书 | 以审计 JSONL 行代替「已通知」占位 |

## 参考

- PRD §7、§9.3.6–9.3.7
