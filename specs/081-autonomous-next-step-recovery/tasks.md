# Tasks：081 Autonomous Next Step Recovery

- [x] T811 formalize `WI-081` owner、spec/plan/tasks 与 `.ai-sdlc` state 映射，令当前分支从已完成的 `WI-080` 切换出来。
- [x] T812 冻结 autonomous next-step 的核心 contract：`propose_recovery` 自动执行、`continue_session` 人工闸门、`runtime_disposition` 兼容映射。
- [x] T813 冻结多项目 progress summary / Feishu delivery 的聚合交付回归，不允许只覆盖首个项目。
- [x] T814 以 targeted verification 收口 `WI-081` 的 runtime evidence，并准备后续 live runtime acceptance 的下一批任务。
