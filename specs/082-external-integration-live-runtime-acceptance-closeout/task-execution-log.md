# Task Execution Log：082-external-integration-live-runtime-acceptance-closeout

- 2026-04-18：基于常驻对抗评审专家 Planck / Leibniz 的合议结论，确认 `WI-082` 只负责 `external-integration live runtime acceptance closeout`，并把 resident expert replay / session-directory / health 作为验收证据链的一部分。
- 2026-04-18：将当前工作分支切换为 `codex/082-external-integration-live-runtime-acceptance-closeout`，不再把后续验收动作回填到已完成的 `WI-081`。
- 2026-04-18：为 `WI-082` 补齐 `spec / plan / tasks / task-execution-log` 以及 `.ai-sdlc/work-items/082-*` formal mirror，并同步修复 `.ai-sdlc/state/checkpoint.yml` 的重复 `last_synced_at` 键。
- 2026-04-18：扩展 live acceptance runbook 与 docs contract，要求 evidence bundle 明确覆盖 `resident_expert_consultation` replay、`resident_expert_coverage` 读面一致性，以及 stale expert 的 degraded health 语义。
- 2026-04-18：运行 targeted suite `uv run pytest -q tests/test_long_running_autonomy_doc_contracts.py tests/test_watchdog_external_integration_smoke.py tests/test_watchdog_replay.py tests/test_watchdog_resident_experts.py tests/test_watchdog_session_spine_api.py::test_session_directory_route_surfaces_resident_expert_coverage tests/test_watchdog_session_spine_runtime.py::test_resident_orchestrator_records_resident_expert_consultation_evidence tests/test_watchdog_session_spine_runtime.py::test_resident_orchestrator_marks_resident_expert_coverage_degraded_when_stale tests/test_watchdog_ops.py::test_watchdog_ops_alerts_and_healthz_report_degraded_status tests/test_watchdog_ops.py::test_watchdog_ops_exposes_stale_resident_expert_runtime_status tests/integration/test_feishu_integration_spine.py::test_integration_session_directory_api_and_adapter_share_resident_expert_coverage`，结果 `40 passed in 0.82s`。
- 2026-04-18：执行 live smoke bundle `set -a; source .env.w; set +a; uv run python scripts/watchdog_external_integration_smoke.py --target health --target feishu --target feishu-control --target provider --target memory --markdown-report artifacts/watchdog-live-acceptance.md`；`health`、`feishu`、`provider`、`memory` 均 `passed`，`feishu-control` 因缺少 `WATCHDOG_SMOKE_FEISHU_CONTROL_PROJECT_ID / WATCHDOG_SMOKE_FEISHU_CONTROL_GOAL_MESSAGE` 被 `skipped`，因此 `T824` 继续保持进行中。
- 2026-04-18：发现 smoke markdown report 把“4 项 passed + 1 项 skipped”错误汇总为 `Overall Status: passed`；已先补红测 `test_render_markdown_report_fails_closed_when_any_selected_target_is_skipped`，再把 report 聚合改为 fail-closed，避免把 `skipped` 误写成整包通过。
- 2026-04-18：重跑 live smoke bundle 后，`artifacts/watchdog-live-acceptance.md` 已正确记录 `Overall Status: failed`；当前 blocker 仍是 `feishu-control` 缺少 `WATCHDOG_SMOKE_FEISHU_CONTROL_PROJECT_ID / WATCHDOG_SMOKE_FEISHU_CONTROL_GOAL_MESSAGE`。
- 2026-04-18：执行 `set -a; source .env.w; export WATCHDOG_SMOKE_FEISHU_DISCOVERY_EXPECTED_PROJECT_IDS="codex-watchdog,Ai_AutoSDLC,meeting,portal"; set +a; uv run python scripts/watchdog_external_integration_smoke.py --target feishu-discovery` 返回 `contract_mismatch`：运行时对 discovery 请求返回 `goal_contract_bootstrap`，而非 `session_directory`。该问题属于新的 runtime residual blocker，按 `FR-8204` 只能记录并保持 `WI-082/T824` 未完成，不能在本验收票内伪装为 closeout 成功。
- 2026-04-18：按 `FR-8204` 正式切出 `WI-083` 承接 `feishu-discovery` 的 session-directory routing residual；`WI-082` 继续保留 fail-closed closeout 结论，不把 discovery blocker 混写为已完成。
- 2026-04-19：结合常驻专家复核，确认 `WI-083` 已关闭 `feishu-discovery` runtime residual，因此 `WI-082/T824` 的当前 blocker 只剩 `feishu-control` 必需环境变量缺失。
- 2026-04-19：执行 `set -a; source .env.w; set +a; export WATCHDOG_HTTP_TIMEOUT_S=15; export WATCHDOG_SMOKE_FEISHU_DISCOVERY_EXPECTED_PROJECT_IDS="codex-watchdog,Ai_AutoSDLC,meeting,portal"; uv run python scripts/watchdog_external_integration_smoke.py --target health --target feishu --target feishu-control --target feishu-discovery --target provider --target memory --markdown-report artifacts/watchdog-live-acceptance.md`；`health`、`feishu`、`provider`、`memory`、`feishu-discovery` 均 `passed`，`feishu-control` 因缺少 `WATCHDOG_SMOKE_FEISHU_CONTROL_PROJECT_ID / WATCHDOG_SMOKE_FEISHU_CONTROL_GOAL_MESSAGE` 被 `skipped`，整包继续 fail-closed 记录为 `Overall Status: failed`。
- 2026-04-19：常驻专家 Schrodinger / Cicero 与本地代码路径复核一致确认：`feishu-control` synthetic smoke 会向 `/api/v1/watchdog/feishu/events` 发送 `repo:<project_id> /goal <goal_message>` 的合成 DM，并进入真实 `goal_contract_bootstrap` 写路径；该链路可能创建/修订 goal contract，并 supersede pending approvals / delivery outbox。因此当前 blocker 需要收敛为“缺少经操作者确认、可安全命中的 live target”，而不能继续把缺失变量理解成可由仓库事实自动补齐的普通配置缺口。
- 2026-04-19：按 TDD 先补红测，再把 `feishu-control` 缺少 live target 的 skip 语义从通用 `feature_not_configured` 收紧为 `operator_confirmation_required`，并在 smoke evidence 中固定输出 `required_action=confirm_mutating_live_target`、`mutation_path=goal_contract_bootstrap`；README 与 `docs/getting-started.zh-CN.md` 也同步更新为“这是写路径、不会自动猜值”。验证命令：`uv run pytest -q tests/test_watchdog_external_integration_smoke.py tests/test_long_running_autonomy_doc_contracts.py`，结果 `30 passed in 0.19s`。
- 2026-04-19：根据常驻专家 Cicero / Schrodinger 的后续定向复核，继续把新引入的 smoke 设计锁进文档契约：`docs/architecture/external-integration-smoke-harness-design.md` 被纳入 `docs_contracts`，README / `docs/getting-started.zh-CN.md` / `docs/operations/external-integration-live-acceptance.md` 同步收紧为必须显式保留 `skipped`、`operator_confirmation_required`、`confirm_mutating_live_target`、`project_id + goal_message` 不可猜填与整包 `fail-closed` 语义；同时补充 `feishu-discovery` 的默认文案 `项目列表` 与 `list_sessions/session_directory` 契约覆盖。验证命令：`uv run pytest -q tests/test_long_running_autonomy_doc_contracts.py tests/test_watchdog_external_integration_smoke.py`、`uv run python -m ai_sdlc verify constraints`、`git diff --check`，结果分别为 `30 passed in 0.22s`、`Constraints OK` 与 clean。
- 2026-04-19：针对 `feishu-control` 的唯一 blocker，按常驻专家 Schrodinger 的只读安全复核，在 live A-Agent 上创建隔离 smoke target `watchdog-smoke-feishu-control-20260419-012852`，保守参数固定为 `status=created`、`phase=planning`、`pending_approval=false`；随后通过 `GET /api/v1/tasks/{project_id}` 确认返回 `thread_id=thr_8f36a2624e6c4f69`，并把它锁定为 `WATCHDOG_SMOKE_FEISHU_CONTROL_EXPECTED_SESSION_ID`。
- 2026-04-19：先执行定向 smoke：`set -a; source .env.w; set +a; export WATCHDOG_HTTP_TIMEOUT_S=15; export WATCHDOG_SMOKE_FEISHU_CONTROL_HTTP_TIMEOUT_S=30; export WATCHDOG_SMOKE_FEISHU_CONTROL_PROJECT_ID=watchdog-smoke-feishu-control-20260419-012852; export WATCHDOG_SMOKE_FEISHU_CONTROL_GOAL_MESSAGE='Smoke validation goal for Feishu goal_contract_bootstrap on dedicated target; do not use for production work.'; export WATCHDOG_SMOKE_FEISHU_CONTROL_EXPECTED_SESSION_ID=thr_8f36a2624e6c4f69; uv run python scripts/watchdog_external_integration_smoke.py --target feishu-control --markdown-report artifacts/watchdog-live-feishu-control.md`，结果 `feishu-control passed`，evidence 固定为 `project_id=watchdog-smoke-feishu-control-20260419-012852`、`session_id=thr_8f36a2624e6c4f69`、`goal_contract_version=goal-v1`。
- 2026-04-19：使用同一隔离 target 重跑完整 live smoke bundle：`set -a; source .env.w; set +a; export WATCHDOG_HTTP_TIMEOUT_S=15; export WATCHDOG_SMOKE_FEISHU_CONTROL_HTTP_TIMEOUT_S=30; export WATCHDOG_SMOKE_FEISHU_CONTROL_PROJECT_ID=watchdog-smoke-feishu-control-20260419-012852; export WATCHDOG_SMOKE_FEISHU_CONTROL_GOAL_MESSAGE='Smoke validation goal for Feishu goal_contract_bootstrap on dedicated target; do not use for production work.'; export WATCHDOG_SMOKE_FEISHU_CONTROL_EXPECTED_SESSION_ID=thr_8f36a2624e6c4f69; export WATCHDOG_SMOKE_FEISHU_DISCOVERY_EXPECTED_PROJECT_IDS='codex-watchdog,Ai_AutoSDLC,meeting,portal'; uv run python scripts/watchdog_external_integration_smoke.py --target health --target feishu --target feishu-control --target feishu-discovery --target provider --target memory --markdown-report artifacts/watchdog-live-acceptance.md`；六项 target 全部 `passed`，`artifacts/watchdog-live-acceptance.md` 汇总为 `Overall Status: passed`，其中 `feishu-control` evidence 为 `goal_contract_version=goal-v2`、`project_id=watchdog-smoke-feishu-control-20260419-012852`、`session_id=thr_8f36a2624e6c4f69`。至此 `WI-082/T824` 的唯一 blocker 已清除并完成 closeout 所需 evidence bundle。
