# Plan：068-feishu-ingress-event-subscription-contract

1. 冻结 `WI-068` owner 边界与 formal docs。
2. 写 failing tests 锁定 Feishu 官方 ingress 的 challenge、文本消息归一与 fail-closed 绑定。
3. 实现官方 webhook ingress route 与 canonical normalization。
4. 跑 targeted verification，送 Hilbert / Gauss 复核。
5. 回写 `ai_sdlc` 状态并 closeout。
