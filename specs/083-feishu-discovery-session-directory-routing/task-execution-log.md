# Task Execution Log：083-feishu-discovery-session-directory-routing

- 2026-04-18：从 `WI-082` 的 live smoke 结果中拆出新 residual blocker：`feishu-discovery` 在真实环境返回 `goal_contract_bootstrap`，而不是 `command_request + list_sessions/session_directory`。
- 2026-04-18：创建并切换到 `codex/083-feishu-discovery-session-directory-routing`，将该问题正式登记为 `WI-083`，避免继续混写在 `WI-082` 的验收 closeout 中。
- 2026-04-18：同步新增 `spec / plan / tasks` 与 `.ai-sdlc/work-items/083-*` mirror，并把 residual ledger 从 `NO_RESIDUAL_BLOCKERS` 更新为由 `WI-083` 承接的 active residual。
- 2026-04-18：比对 `src/watchdog/services/feishu_ingress/service.py`、`src/watchdog/services/adapters/openclaw/intents.py` 与 live smoke 证据，冻结 `WI-083` 边界：repo-local ingress 已将 `项目列表` / `所有项目进展` 识别为 `GLOBAL_READ_INTENTS -> list_sessions`，而真实环境仍把 discovery 文本当作 goal bootstrap 处理，因此 residual 不再是文档表述问题，而是 live routing/deploy drift。
- 2026-04-18：按 TDD 先新增 `tests/test_watchdog_external_integration_smoke.py::test_feishu_discovery_check_uses_documented_default_command_text`，证明 smoke 默认 discovery 文本与运行文档漂移；修复 `src/watchdog/validation/external_integration_smoke.py` 后，targeted suite `uv run pytest -q tests/test_watchdog_external_integration_smoke.py -k 'documented_default_command_text or feishu_discovery_check_verifies_expected_project_ids or cli_can_write_markdown_report_artifact or fails_closed_when_any_selected_target_is_skipped'` 结果 `4 passed`。
- 2026-04-18：补充 alias 回归 `tests/test_watchdog_feishu_ingress.py::test_feishu_ingress_discovery_alias_routes_to_session_directory`，并执行 `uv run pytest -q tests/test_watchdog_feishu_ingress.py -k 'global_project_directory_command_skips_project_binding or discovery_alias_routes_to_session_directory or project_directory_surfaces_next_steps_for_pending_approval or lists_projects_with_relative_freshness'`，结果 `4 passed`。
- 2026-04-18：继续补充与 live 症状严格对齐的护栏：新增 `tests/test_watchdog_feishu_ingress.py::test_feishu_ingress_default_bound_project_directory_stays_command_request` 与 `::test_feishu_ingress_default_bound_discovery_alias_stays_command_request`，证明即使配置了 `default_project_id`，repo-local `项目列表` / `所有项目进展` 也必须优先命中 `GLOBAL_READ_INTENTS`，不能退回 `goal_contract_bootstrap`；targeted suite `uv run pytest -q tests/test_watchdog_feishu_ingress.py -k 'default_bound_status_stays_command_request or default_bound_project_directory_stays_command_request or default_bound_discovery_alias_stays_command_request or global_project_directory_command_skips_project_binding or discovery_alias_routes_to_session_directory'` 结果 `5 passed`。
- 2026-04-18：再次执行 live smoke：`set -a; source .env.w; export WATCHDOG_SMOKE_FEISHU_DISCOVERY_EXPECTED_PROJECT_IDS=\"openclaw-codex-watchdog,Ai_AutoSDLC,meeting,portal\"; set +a; uv run python scripts/watchdog_external_integration_smoke.py --target feishu-discovery`，真实环境仍返回 `accepted=true` 但 `event_type=goal_contract_bootstrap`、`project_id=openclaw-codex-watchdog`，未满足 `command_request + session_directory` contract，因此 `WI-083` 暂不能 closeout。
- 2026-04-18：直接对 live `/api/v1/watchdog/feishu/events` 发送 synthetic event 做差异诊断：`状态` 返回 `command_request + get_session`，但 `项目列表` 与 `所有项目进展` 都返回 `goal_contract_bootstrap(project_id=openclaw-codex-watchdog, session_id=session:openclaw-codex-watchdog)`；同时 `/healthz` 只暴露 `{status: degraded, active_alerts, release_gate_blockers}`，没有版本/commit 指纹。结论是 live 并非整体命令入口失效，而是缺失 `list_sessions/GLOBAL_READ_INTENTS` 识别或仍在运行旧部署。
- 2026-04-19：追查 launchd 部署路径后，确认本机 `com.openclaw.watchdog` 仍绑定当前仓库，但进程启动时间早于 repo-local 修复；执行 `launchctl kickstart -k gui/$(id -u)/com.openclaw.watchdog` 后，live `/api/v1/watchdog/feishu/events` 重新返回 `项目列表 -> command_request + list_sessions + session_directory`，说明先前 residual 的根因是本机 launchd 服务仍停留在旧进程。
- 2026-04-19：重启后 live discovery 已按 contract 返回，但真实 `session_directory` 响应在当前数据量下需要约 12.5s 到 14.7s；`WATCHDOG_SMOKE_FEISHU_DISCOVERY_HTTP_TIMEOUT_S` 的默认 15s 对 `httpx` 读超时过于贴边。于是将 `src/watchdog/validation/external_integration_smoke.py` 与 `scripts/watchdog_external_integration_smoke.py` 的 discovery 默认超时提升到 30s，并同步 README / getting-started / operations 文档与 `config/examples/watchdog.env.example`。
- 2026-04-19：补充回归 `tests/test_watchdog_external_integration_smoke.py::test_cli_uses_default_feishu_discovery_timeout_when_env_absent`，并执行 `uv run pytest -q tests/test_watchdog_external_integration_smoke.py -k 'render_markdown_report_redacts_secret_values_and_includes_status or cli_reads_feishu_discovery_env or cli_uses_default_feishu_discovery_timeout_when_env_absent or documented_default_command_text or feishu_discovery_check_verifies_expected_project_ids or fails_closed_when_any_selected_target_is_skipped'`，结果 `6 passed`。
- 2026-04-19：再次执行 live smoke：`set -a; source .env.w; export WATCHDOG_SMOKE_FEISHU_DISCOVERY_EXPECTED_PROJECT_IDS="openclaw-codex-watchdog,Ai_AutoSDLC,meeting,portal"; set +a; uv run python scripts/watchdog_external_integration_smoke.py --target feishu-discovery`，结果 `passed`；返回 `command_request + list_sessions + session_directory`，且项目集合覆盖 `openclaw-codex-watchdog / Ai_AutoSDLC / meeting / portal`，`WI-083` 关闭。
